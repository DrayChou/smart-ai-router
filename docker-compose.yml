# Smart AI Router - 简单Docker Compose配置

services:
  smart-ai-router:
    build: .
    container_name: smart-ai-router
    restart: unless-stopped
    ports:
      - "7601:7601"
    environment:
      - LOG_LEVEL=INFO
      - DEBUG=false
    volumes:
      - ./config:/app/config:ro
      - ./logs:/app/logs
      - ./cache:/app/cache
      - ./.env:/app/.env:ro
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:7601/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  default:
    name: smart-ai-router